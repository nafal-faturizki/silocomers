# Environment example for local development
# Copy to .env.local and fill values

# Cloudflare
CF_ACCOUNT_ID=
CF_API_TOKEN=

# Database / D1 (if used locally)
D1_DB_PATH=./dev-data/db.sqlite

# App settings
NODE_ENV=development
PORT=3000
